# ================================
# Flavor Studios CI environment template
# Use in your CI secret manager or run `pnpm env:check:strict` locally.
# ================================

# CI context
CI=true
NODE_ENV=production
VERCEL_ENV=preview

# App origins
BASE_URL=https://example.com
NEXT_PUBLIC_BASE_URL=https://example.com

# Core server secrets (required in all stages)
CRON_SECRET=__REQUIRED__
PREVIEW_SECRET=__REQUIRED__
ADMIN_JWT_SECRET=__REQUIRED__

# ------------------------------------------------
# Firebase Web SDK (client) — all are REQUIRED
# ------------------------------------------------
NEXT_PUBLIC_FIREBASE_API_KEY=__REQUIRED__
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-app.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-app
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-app.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=__REQUIRED__
NEXT_PUBLIC_FIREBASE_APP_ID=__REQUIRED__
# Optional analytics key
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# ------------------------------------------------
# Firebase Admin (server) — provide ONE of these
# ------------------------------------------------
# Storage bucket must match the client bucket above
FIREBASE_STORAGE_BUCKET=your-app.appspot.com

# Option A: raw JSON (single line)
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-app","private_key_id":"<id>","private_key":"-----BEGIN PRIVATE KEY-----\\n...","client_email":"firebase-adminsdk@your-app.iam.gserviceaccount.com","client_id":"123456789"}

# Option B: full JSON payload (alternative to KEY)
#FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"your-app","private_key_id":"<id>","private_key":"-----BEGIN PRIVATE KEY-----\\n...","client_email":"firebase-adminsdk@your-app.iam.gserviceaccount.com","client_id":"123456789"}

# ------------------------------------------------
# Validator/test toggles (keep strict in CI)
# ------------------------------------------------
SKIP_STRICT_ENV=0
E2E=false
USE_DEFAULT_ENV=0
