"use client";
import { useEffect } from "react";

/**
 * Development-only helpers for the admin app.
 * Currently acts as a no-op placeholder so the build succeeds.
 */
export default function DevTools() {
  useEffect(() => {
    // Intentionally left blank for future development utilities.
  }, []);

  return null;
}