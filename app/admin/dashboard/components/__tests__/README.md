# Admin dashboard component tests

These tests ensure critical admin UI pieces render and respond to user actions while mocking API calls. The goal is to maintain roughly **70% statement coverage** for the header, sidebar, overview and blog manager components.