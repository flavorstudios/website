# Keep backend Docker builds lean when using this folder as the context
node_modules
**/.next
**/dist
.pnpm-store
.pnp.*
.DS_Store
npm-debug.log
yarn-debug.log
yarn-error.log
pnpm-debug.log

# Allow workspace manifests and sources needed for builds
!pnpm-lock.yaml
!pnpm-workspace.yaml
!package.json
!tsconfig.json
!shared/**
!Dockerfile

# Re-exclude nested dependency folders
shared/node_modules
