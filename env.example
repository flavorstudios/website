# ================================
# Flavor Studios Example .env File
# ================================

# --- Site Base URL ---
NEXT_PUBLIC_BASE_URL=https://flavorstudios.in
BASE_URL=https://flavorstudios.in

# --- Admin Emails (REQUIRED for secure Google login/admin access) ---
# SERVER-ONLY: DO NOT EXPOSE TO CLIENT CODE OR PUBLIC REPOS!
# Comma-separated for multiple admins:
#   ADMIN_EMAILS=admin@flavorstudios.in,another.admin@flavorstudios.in
# Optionally allow all users at a domain (use ONLY if truly needed):
#   ADMIN_DOMAIN=flavorstudios.in
ADMIN_EMAILS=your_admin_email_here
# ADMIN_DOMAIN=your_admin_domain_here   # (Optional: allow all @domain users as admin)

# WARNING: DO NOT set a public (NEXT_PUBLIC_) admin email variable.
# WARNING: NEVER expose admin email(s) to the browser bundle or share publicly.

# --- Bing Search API Key ---
BING_API_KEY=your_bing_api_key_here

# --- IndexNow API Key (for instant search indexing) ---
INDEXNOW_KEY=your_indexnow_key_here

# --- Firebase Web App (Client-side) ---
# These MUST be set in both Vercel and GitHub Actions for correct deployment
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain_here
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_firebase_project_id_here
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_firebase_storage_bucket_here
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_firebase_messaging_sender_id_here
NEXT_PUBLIC_FIREBASE_APP_ID=your_firebase_app_id_here
NEXT_PUBLIC_FIREBASE_VAPID_KEY=your_firebase_vapid_key_here

# --- Firebase Admin SDK (for server-side only, as JSON string) ---
# WARNING: NEVER expose this key to the browser/client. Set ONLY in Vercel (Server) or GitHub Secrets.
FIREBASE_SERVICE_ACCOUNT_KEY=your_firebase_service_account_key_json_here

# --- Perspective Comment Analyzer API ---
PERSPECTIVE_API_KEY=your_perspective_api_key_here

# --- Local SQLite Database (Prisma) ---
DATABASE_URL="file:./dev.db"

# --- Any other project-specific environment variables go here ---

# ========================================
# Additional Admin Auth (Codex recommendations)
# ========================================

# Optional admin email/password login (bcrypt hash of password, NOT plain text)
ADMIN_PASSWORD_HASH=your_bcrypt_hash_here

# Shared secret for JWT admin session cookies
ADMIN_JWT_SECRET=your_jwt_secret_here

# Optional TOTP secret for admin 2FA (e.g. for Google Authenticator)
ADMIN_TOTP_SECRET=your_totp_secret_here

# ========================================

# (Legacy password auth lines have been permanently removed as recommended by Codex Audit.)
